package examples

import examples.{Tuples, Lists, Maps, Sets, Arrays}

object TestCollections {
  def main(args: Array[String]): Unit = {
    val tuples = Tuples
    val lists = Lists
    val maps = Maps
    val sets = Sets
    val arrays = Arrays
  }
}
